spring:
  application:
    name: content
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/xstudy_content?serverTimezone=UTC&userUnicode=true&useSSL=false&
    username: root
    password: password
logging:
  config: classpath:log4j2-dev.xml
